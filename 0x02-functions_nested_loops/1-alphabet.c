#include "main.h"
/**
 * main - check the code
 *
 *Description: tells what prototype print_alphabet does
 *
 * Return: Always 0
 */

void print_alphabet(void)
{
	char(x);

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar (x);
	}
	putchar ('\n');
	return (0);
}
