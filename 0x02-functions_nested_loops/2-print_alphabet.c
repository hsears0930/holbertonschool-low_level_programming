#include <stdio.h>
/**
 * main - entry point
 *
 * description - putchar to print lowercase alphabet
 *
 * return: Always 0 (Success)
 */

int main(void)
{
	char(x);

	for (x = 'a'; a <= 'z'; x++)
	{
		putchar (x);
	}
	putchar ('\n');

	return(0);
}
