#include <stdio.h>
/**
 * main - entry point
 *
 * Description: printing numbers through ten
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x = 0;

		while
			(x < 10);
				(x++);
				return (0);
}
