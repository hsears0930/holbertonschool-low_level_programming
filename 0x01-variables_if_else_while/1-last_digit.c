#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/** 
 * printf - displays the desired text
 * scanf - reads the desired variable 
 * %d - decimal value 
 */
int main(void)
{
	int n;

	srand(time(0);
			n = rand() - RAND_MAX / 2;
			printf("
