#include <stdio.h>
/**
 * main - entry point
 *
 * Description: putchar to count all hexidecimals in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char(x);
	char(N);

	for (x = 30; x <= 39; x++)
	{
		putchar (x);
	}
	for (N = 61; N <= 66; N++)
	{
		putchar (N);
	}
	putchar ('\n');

	return (0);
}
