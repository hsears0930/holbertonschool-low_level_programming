#include <stdio.h>
/**
 * main - entry point
 *
 * Description: putchar to count all hexidecimals in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char(x);
	char(N);

	for (x = '0'; x <= '9'; x++)
	{
		putchar (x);
	}
	for (N = 'a'; N <= 'f'; N++)
	{
		putchar (N);
	}
	putchar ('\n');

	return (0);
}
