#include <stdio.h>
/**
 * main - entry point
 *
 * Description: putchar to print lowercase alphabet
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char(x);

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar (x);
	}
	putchar ('\n');

	return (0);
}
